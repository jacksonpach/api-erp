import {DB_DIALECT, DB_HOST, DB_PASSWORD, DB_PORT, DB_USER} from '../config/config';

export const databaseConfig = {
    dialect: DB_DIALECT,
    host: DB_HOST,
    username: DB_USER,
    password: DB_PASSWORD,
    database: DB_PASSWORD,
    port: DB_PORT,
};